export type csvLine = {
  PMID: number;
  Email: string;
  Affiliation: string;
  Department: string;
  Institution: string;
};

export type csvEntry = {
  Email: string;
  Affiliation: string;
  Department: string;
  Institution: string;
};